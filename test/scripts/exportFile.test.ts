//const { excelUtil } = require('../../scripts/exportFile')

describe("test add function", () => {

    it("should return 15 for add(10,5)", () => {
        //excelUtil.createFile('Object-Infos.xlsx', combinedMetadata, null);
    });
    it("should return 5 for add(2,3)", () => {

    });
});